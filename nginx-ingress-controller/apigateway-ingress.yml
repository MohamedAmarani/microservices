apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    #nginx.ingress.kubernetes.io/rewrite-target: /
  name: apigateway-ingress
spec:
  rules:
    - host: ec2-15-237-24-182.eu-west-3.compute.amazonaws.com
      http:
        paths:
          - path: /api/v1
            pathType: Prefix
            backend:
              service:
                name: apigateway-service
                port:
                  number: 8080
