kind: Service
apiVersion: v1
metadata:
  name: account-service
spec:
  selector:
    app: accountse
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 8080
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: accountse
  template:
    metadata:
      labels:
        app: accountse
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/path: 'actuator/prometheus'
    spec:
      containers:
        - name: accountse
          image: scaredste/account-service:v7.0.6
          ports:
            - containerPort: 8080
              protocol: TCP
